<%= form_for @post do |f| %>

  <%= f.text_field :comment %>

  <%= f.fields_for :comments, @post.comments.build do |comments_fields| %>
   <%= comments_fields.text_field :comment_content %>
  <% end %>

  <%= f.submit %>
<% end %>
